import { style } from "typestyle"

const main = style({
  width: "100%"
})

const center = style({
  textAlign: "center",
  margin: "0 auto"
})

const MapStyles = {
  main,
  center
}

export default MapStyles